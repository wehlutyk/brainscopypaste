# Needed first, for the full requirements install to work.
# If you don't believe in this try `pip install -r requirements.txt`
# directly and see what error it gives you.
numpy==1.8.0
wsgiref==0.1.2
